import { hot } from 'react-hot-loader/root';
import "./App.css";

const App = () => {
    return(
      <div className="App">
        <h1> Hello, Losers! </h1>
      </div>
    );
}

export default hot(App);
